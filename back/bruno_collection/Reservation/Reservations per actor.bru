meta {
  name: Reservations per actor
  type: http
  seq: 10
}

get {
  url: http://{{url}}:{{port}}/{{baseRoute}}/reservation/actor/:actorId
  body: none
  auth: inherit
}

params:path {
  actorId: pg202404
}

settings {
  encodeUrl: true
  timeout: 0
}
