meta {
  name: Cancel reservation
  type: http
  seq: 2
}

post {
  url: http://{{url}}:{{port}}/{{baseRoute}}/reservation/cancel/
  body: json
  auth: inherit
}

body:json {
  {
    "reservationId": 12,
    "actorId": "pg202404"
  }
  
  
  
  
  
  
  
  
}

settings {
  encodeUrl: true
  timeout: 0
}
