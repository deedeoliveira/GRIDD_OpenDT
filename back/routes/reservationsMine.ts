import express from 'express'; import MySQLDatabase from '../utils/mysqlDatabase.ts'; import { buildErrorResponse,buildSuccessResponse } from '../utils/responseHandler.ts'; import { applicationIdentityRuntime } from '../applicationIdentity/applicationIdentityMiddleware.ts'; import { studentReservationsSql,toStudentReservation } from '../reservationApproval/studentReservationDecision.ts';
const router=express.Router(); const db=new MySQLDatabase();
router.get('/mine',async(req,res)=>{if(applicationIdentityRuntime().config.mode!=='local_session'||!req.applicationIdentity)return buildErrorResponse(res,401,'A local development session is required.');try{await db.checkConnection();const [rows]:any=await db.connection.execute(studentReservationsSql,{accountId:req.applicationIdentity.accountId});return buildSuccessResponse(res,200,rows.map(toStudentReservation));}catch{return buildErrorResponse(res,500,'Reservations could not be listed.');}}); export default router;
