import { NextResponse } from "next/server";
async function proxy(req:Request,{params}:{params:Promise<{path:string[]}>}){ const {path}=await params; const headers=new Headers(); const cookie=req.headers.get('cookie'); if(cookie) headers.set('cookie',cookie); if(req.method!=='GET') headers.set('content-type','application/json'); const response=await fetch(`${process.env.BASE_API_URL}/auth/${path.join('/')}`,{method:req.method,headers,body:req.method==='GET'?undefined:await req.text()}); const body=await response.text(); const out=new NextResponse(body,{status:response.status,headers:{'content-type':'application/json'}}); const setCookie=response.headers.get('set-cookie'); if(setCookie) out.headers.set('set-cookie',setCookie); return out; }
export const GET=proxy; export const POST=proxy;
